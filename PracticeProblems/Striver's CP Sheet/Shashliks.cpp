#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main(){
  
  int t;
  ll k, a, b, x, y;
  cin >> t;

  for(int i = 0; i < t; i++){
    cin >> k >> a >> b >> x >> y;

    ll m = (k - a) / x + 1;
    ll t1 = k - x * m;

    int 
  }
  
}